import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#0a1923',
        },
        secondary: {
            main: '#fff',
        },
        background: {
            paper: '#0a1923',
            default: '#0a1923'
        }
    },
});

export default theme;